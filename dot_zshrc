#!/usr/bin/env zsh

# ================================
# Migue's Personal Zsh Configuration
# ================================

# Source all .zshrc.d files if they exist
if [ -d ~/.zshrc.d ]; then
    for rc in ~/.zshrc.d/*; do
        if [ -f "$rc" ]; then
            source "$rc"
        fi
    done
fi
